# "The Closed Laboratory" Lab Note — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Publish a new Ludoratory lab note, "The Closed Laboratory," about the Ligas do Brasil football-reform project, plus the one small `GameCard` change the note's project card needs.

**Architecture:** Follows the established lab-note pipeline — a Markdown/MDX file in `src/content/lab-notes/` (title from the first `# heading`, date from the filename), post images under `public/lab-notes/<slug>/`, and an OG card generated by `npm run build:og`. One reusable-component change: `GameCard` gains an optional `ctaLabel` prop so its hover overlay can say "Read the manifesto ↗" instead of the hardcoded "Play now ↗".

**Tech Stack:** Vite + React 19 + TypeScript, MUI, `@mdx-js/rollup`, Vitest + `@testing-library/react`, `cwebp` (Homebrew) for the webp sibling image.

## Global Constraints

- Post file path: `src/content/lab-notes/2026-07-24-the-closed-laboratory.md` (date-prefixed, kebab slug).
- Slug (derived from filename): `the-closed-laboratory`. Post image folder: `public/lab-notes/the-closed-laboratory/`.
- English only. The linked manifesto is Portuguese; this note is English *about* it.
- Copy rules: exactly **2** handwritten notes (`<NoteUp>` §1, `<NoteDown>` §2); one pull-quote (`> Population was never the bottleneck. The architecture was.`); translate *craque* → *star* (no untranslated loanword).
- Card = option A: a single project `<GameCard>` at the end. No specimen cards.
- `ctaLabel` default MUST remain `"Play now ↗"` so existing card call sites are unaffected.
- Build command is `tsc -b && vite build && node scripts/rewrite-og-html.mjs`; tests run via `npm test` (`vitest run --passWithNoTests`).

---

### Task 1: Add `ctaLabel` prop to `GameCard`

**Files:**
- Modify: `src/components/GameCard.tsx` (props interface ~line 6-17; overlay chip text ~line 89-91)
- Test: `src/components/GameCard.test.tsx` (create)

**Interfaces:**
- Consumes: nothing (leaf component).
- Produces: `GameCardProps` gains `ctaLabel?: string`. When `url` is set, the hover overlay chip renders `ctaLabel ?? "Play now ↗"`. Existing props unchanged.

- [ ] **Step 1: Write the failing test**

Create `src/components/GameCard.test.tsx`:

```tsx
import { render, screen } from "@testing-library/react";
import { GameCard } from "./GameCard";

describe("GameCard ctaLabel", () => {
  it("renders the default overlay label when ctaLabel is omitted", () => {
    render(
      <GameCard name="Some Game" image="/x.png" url="https://example.com">
        A game.
      </GameCard>,
    );
    expect(screen.getByText("Play now ↗")).toBeInTheDocument();
  });

  it("renders a custom overlay label when ctaLabel is provided", () => {
    render(
      <GameCard
        name="Ligas do Brasil"
        image="/x.png"
        url="https://example.com"
        ctaLabel="Read the manifesto ↗"
      >
        A manifesto.
      </GameCard>,
    );
    expect(screen.getByText("Read the manifesto ↗")).toBeInTheDocument();
    expect(screen.queryByText("Play now ↗")).not.toBeInTheDocument();
  });
});
```

- [ ] **Step 2: Run the test to verify it fails**

Run: `npx vitest run src/components/GameCard.test.tsx`
Expected: FAIL — the second test can't find "Read the manifesto ↗" (overlay is still hardcoded "Play now ↗").

- [ ] **Step 3: Add the prop to the interface**

In `src/components/GameCard.tsx`, add to `GameCardProps` (after `players?: string;`):

```tsx
  players?: string;
  /** Overlay chip label shown on hover when `url` is set. Defaults to "Play now ↗". */
  ctaLabel?: string;
```

- [ ] **Step 4: Destructure and use it**

In the `GameCard` function signature, add `ctaLabel = "Play now ↗",` alongside the other destructured props (e.g. right after `players,`). Then replace the hardcoded overlay chip text:

```tsx
            {ctaLabel}
```

(the line currently reading `Play now ↗` inside the `.game-card-overlay-chip` Box).

- [ ] **Step 5: Run the test to verify it passes**

Run: `npx vitest run src/components/GameCard.test.tsx`
Expected: PASS (both tests).

- [ ] **Step 6: Typecheck**

Run: `npx tsc -b`
Expected: no errors.

- [ ] **Step 7: Commit**

```bash
git add src/components/GameCard.tsx src/components/GameCard.test.tsx
git commit -m "feat(game-card): add ctaLabel prop for non-game overlay text"
```

---

### Task 2: Add the Ligas do Brasil card image assets

**Files:**
- Create: `public/lab-notes/the-closed-laboratory/ligas-do-brasil.png`
- Create: `public/lab-notes/the-closed-laboratory/ligas-do-brasil.webp`

**Interfaces:**
- Consumes: nothing.
- Produces: a 1200×630 card image (+ webp sibling) referenced by the post's `<GameCard image="/lab-notes/the-closed-laboratory/ligas-do-brasil.png">`. `GameCard` emits `<source srcSet="…ligas-do-brasil.webp" type="image/webp">`, so the webp sibling must exist.

- [ ] **Step 1: Copy the project OG card into the post folder**

```bash
mkdir -p public/lab-notes/the-closed-laboratory
cp ../futebol-br/assets/img/og.png public/lab-notes/the-closed-laboratory/ligas-do-brasil.png
```

- [ ] **Step 2: Generate the webp sibling**

```bash
cwebp -q 82 public/lab-notes/the-closed-laboratory/ligas-do-brasil.png \
  -o public/lab-notes/the-closed-laboratory/ligas-do-brasil.webp
```

- [ ] **Step 3: Verify both files exist and dimensions are 1200×630**

Run:
```bash
node -e "const b=require('fs').readFileSync('public/lab-notes/the-closed-laboratory/ligas-do-brasil.png');console.log('png',b.readUInt32BE(16),b.readUInt32BE(20))"
ls -la public/lab-notes/the-closed-laboratory/
```
Expected: `png 1200 630`, and both `.png` and `.webp` listed.

- [ ] **Step 4: Commit**

```bash
git add public/lab-notes/the-closed-laboratory/
git commit -m "feat(lab-notes): add Ligas do Brasil card image"
```

---

### Task 3: Write the lab note

**Files:**
- Create: `src/content/lab-notes/2026-07-24-the-closed-laboratory.md`

**Interfaces:**
- Consumes: `<NoteUp>`, `<NoteDown>`, `<GameCard>` from `MdxProvider`; the card image from Task 2; the `ctaLabel` prop from Task 1.
- Produces: a lab note whose title (from `# The Closed Laboratory`) and date (`2026-07-24` from the filename) surface on `/lab-notes` and `/lab-notes/the-closed-laboratory`.

- [ ] **Step 1: Write the post file**

Create `src/content/lab-notes/2026-07-24-the-closed-laboratory.md` with exactly this content:

```mdx
# The Closed Laboratory

Romário came from Olaria. Ronaldo from São Cristóvão. Rivaldo from Mogi Mirim. The players who won Brazil its last two World Cups were grown in clubs so small that most Brazilians under thirty have never heard of them — clubs that today, if they exist at all, play a couple of months of a state championship and then go dark until the next year.

<NoteUp>forged in clubs that vanish from the calendar by April</NoteUp>

That's the part that stayed with me. Not the trophies — the soil. Brazil didn't produce its greatest stars *despite* those little clubs. It produced them *because* of them. Hundreds of teams playing real games, all year, across a country the size of a continent: that was a machine for turning teenagers into stars. Nobody built the machine on purpose. It just ran.

## Nobody playtested this

Here's what I keep coming back to, and it's a very Ludoratory way to see it: a football league is a game. Not the ninety minutes on the pitch — the *structure* around them. How many clubs there are, who plays whom, who gets promoted, who spends months with no match at all. Those are rules, and rules have consequences nobody votes on. Change the rules and you change what the system produces, whether you meant to or not.

In 2003, Brazil changed the rules. It swapped its national championship for the tidy European format — one long league table, *pontos corridos* — and quietly buried the sprawling interstate cups that used to keep smaller clubs playing meaningful games for most of the year.

<NoteDown>nobody playtested this patch</NoteDown>

It looked like modernization. In the language of games, it was a balance patch shipped without testing. Someone edited the rules that fed the machine, and nobody checked what happened to the machine.

## The machine, switched off

You can read what happened in the numbers, and it isn't subtle. Before 2003, Brazil had won five World Cups; since, none. Five Ballon d'Or seasons between 1997 and 2007 — Ronaldo, Rivaldo, Ronaldinho, Kaká — and then eighteen straight years without a single one. The average age at which Brazil sells its best players abroad has fallen to nineteen: Endrick left at sixteen, Estêvão at seventeen. The club stopped forging stars. It became an assembly line for prospects.

No single line proves cause. But five independent curves all bend at the same moment, right after the same structural change, and it gets hard to keep calling that a coincidence. The machine still spins — Brazilian football has never earned more money — it just stopped producing the thing it existed for.

> Population was never the bottleneck. The architecture was.

## Reopening the lab

So what do you do with a machine you switched off by accident? You look at the rules again — on purpose this time.

The proposal behind *Ligas do Brasil* is a redesign, not a tweak. Replace the twenty-club national elite and the fragmented state championships with **six regional leagues**, each roughly the population of a whole European country — the smallest of them, the Amazon league, is the size of the Netherlands. Above them, one **open, Champions-League-style national cup** that any club can reach, deciding the Brazilian champion in a concentrated national knockout. A country the size of a continent, it turns out, already deserves a continental-scale competition.

The elegant part is that it doesn't ask for more money. Brazilian football is already awash in it — and drowning in debt at the same time, with five clubs holding half the revenue. The reform just distributes what already exists: more clubs with real calendars, fewer flights, more local derbies, and a structure that keeps hundreds of small clubs playing games that matter all year long. It reopens the laboratory.

## Understanding fun by building it

Ludoratory's whole premise is that you understand a thing by building it — and, sometimes, by proposing how to rebuild it. I can't rebuild Brazilian football. But I could take the system apart, argue for a different architecture, and then — because a manifesto that can't survive contact with reality is just a wish — build a simulator to check the new rules actually hold. Two hundred and sixteen clubs, a full season, a calendar that has to close inside forty-seven usable weeks without leaving anyone unemployed in April. It closes.

The manifesto is in Portuguese, and it goes far deeper than this note — the data, the objections, the money. If this is the kind of system you like to take apart, go read it.

<GameCard
  name="Ligas do Brasil"
  image="/lab-notes/the-closed-laboratory/ligas-do-brasil.png"
  url="https://raphaelaleixo.github.io/ligas-do-brasil/"
  repoUrl="https://github.com/raphaelaleixo/ligas-do-brasil"
  ctaLabel="Read the manifesto ↗"
  note="reopen the lab"
>
  A manifesto — and the season simulator that backs it — for restructuring Brazilian football into six regional leagues and one open, Champions-League-style national cup.
</GameCard>
```

- [ ] **Step 2: Verify the note renders in dev**

Run `npm run dev` and open `http://localhost:5173/lab-notes/the-closed-laboratory`. Confirm:
- Title "The Closed Laboratory" renders as the `<h1>`.
- Both margin scribbles appear (the up-arrow note in §1, the down-arrow note before "It looked like modernization").
- The pull-quote renders in the magenta Caveat blockquote style.
- The card at the bottom shows the Ligas do Brasil image; hovering shows the **"Read the manifesto ↗"** chip (not "Play now"); the footer shows a **code ↗** link.
- The post also appears in the list at `http://localhost:5173/lab-notes`.

Then stop the dev server.

- [ ] **Step 3: Commit**

```bash
git add src/content/lab-notes/2026-07-24-the-closed-laboratory.md
git commit -m "feat(lab-notes): write \"The Closed Laboratory\" post"
```

---

### Task 4: Generate the OG card and verify the production build

**Files:**
- Create: `public/og/notes/the-closed-laboratory.png` (generated)
- Modify: `public/og/notes/.manifest.json` (generated)

**Interfaces:**
- Consumes: the committed note from Task 3 (the OG script auto-discovers notes and reads the title).
- Produces: a per-note OG PNG + manifest entry, served as a static asset.

- [ ] **Step 1: Generate the OG card**

Run: `npm run build:og`
Expected: it spins up a temporary Vite dev server, screenshots the note's OG page, and writes `public/og/notes/the-closed-laboratory.png` plus a new entry in `public/og/notes/.manifest.json`. Other notes are skipped (unchanged hash).

- [ ] **Step 2: Verify the OG artifacts exist**

Run:
```bash
ls -la public/og/notes/the-closed-laboratory.png
grep the-closed-laboratory public/og/notes/.manifest.json
```
Expected: the PNG exists and the slug appears in the manifest.

- [ ] **Step 3: Run the full build and test suite**

Run:
```bash
npm test
npm run build
```
Expected: tests pass (including the GameCard tests from Task 1); `tsc -b` and `vite build` complete with no errors; `rewrite-og-html.mjs` runs clean.

- [ ] **Step 4: Commit**

```bash
git add public/og/notes/the-closed-laboratory.png public/og/notes/.manifest.json
git commit -m "chore(og): generate OG card for \"The Closed Laboratory\""
```

---

## Self-Review

**Spec coverage:**
- Spine / lens (football as designed system, closed laboratory) → Task 3 prose. ✓
- 5-section structure → Task 3 (cold open / "Nobody playtested this" / "The machine, switched off" / "Reopening the lab" / "Understanding fun by building it"). ✓
- Exactly 2 notes, one pull-quote, *craque* → *star* → Task 3 content + Global Constraints. ✓
- Card option A + `ctaLabel` prop → Task 1 (prop) + Task 3 (usage). ✓
- Card image incl. webp sibling → Task 2. ✓
- OG card generation → Task 4. ✓
- Out of scope (no specimen cards, no PT version, no manifesto re-argument) → honored; none added. ✓

**Placeholder scan:** No TBD/TODO; all code and prose are complete and literal. ✓

**Type consistency:** `ctaLabel?: string` defined in Task 1 is the exact prop passed in Task 3. Default `"Play now ↗"` matches the existing hardcoded string so other call sites are unchanged. Image path `/lab-notes/the-closed-laboratory/ligas-do-brasil.png` is identical in Task 2 (asset) and Task 3 (reference). ✓
